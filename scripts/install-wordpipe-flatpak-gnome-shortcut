#!/usr/bin/env sh
set -eu

ROOT="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
APP_ID="${WORDPIPE_FLATPAK_APP_ID:-dev.wordpipe.Wordpipe}"
BINDING="${1:-<Control><Alt>space}"

exec "$ROOT/scripts/wordpipe-dev" shortcut-install \
  --target flatpak \
  --app-id "$APP_ID" \
  --binding "$BINDING"
